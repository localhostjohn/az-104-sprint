import type {Metadata} from 'next';import './globals.css';export const metadata:Metadata={title:'AZ-104 Sprint | Train to 800',description:'Fast, exam-weighted Azure Administrator quiz sprints with instant explanations.',openGraph:{title:'AZ-104 Sprint — Train to 800',description:'Ten-question sprints for the Microsoft Azure Administrator exam.',images:['/og.png']},twitter:{card:'summary_large_image',title:'AZ-104 Sprint — Train to 800',description:'Ten-question sprints for AZ-104.',images:['/og.png']}};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
